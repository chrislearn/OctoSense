# 05 · 数据模型

持久化文件：`$MAKEPAD_HOME/liyu/state.json`（micro_serde，宽松反序列化，字段缺失用默认值）。
枚举一律存 `u8`，金额存「分」`i64`，日期存「天序号」`i64`（沿用原 `data.rs` 的日期工具）。

## 实体

### 礼物目录 `CatalogItem`（代码常量，不持久化）

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | &str | 三顿半精品咖啡礼盒 |
| `cat` | `Category` | Coffee / Movie / Trendy / Blind / Sweet |
| `physical` | bool | 实物（要地址）/ 电子券 |
| `spec` | &str | 24 颗装 · 包邮 |
| `price` | i64 | 分 |

目录 12 件（`CATALOG`，下标就是 `item` 编号，只追加不重排）：

| # | 名称 | 品类 | 形态 | 价格 |
| --- | --- | --- | --- | --- |
| 0 | 三顿半精品咖啡礼盒 | 咖啡茶饮 | 实物 | ¥109 |
| 1 | 星巴克中杯拿铁电子券 | 咖啡茶饮 | 电子券 | ¥35 |
| 2 | 喜茶多肉葡萄兑换券 | 咖啡茶饮 | 电子券 | ¥29 |
| 3 | 电影通兑票 | 电影演出 | 电子券 | ¥49 |
| 4 | 电影双人套票 | 电影演出 | 电子券 | ¥98 |
| 5 | 帆布托特包 | 潮流小物 | 实物 | ¥79 |
| 6 | 香薰蜡烛 | 潮流小物 | 实物 | ¥88 |
| 7 | 拍立得相纸 | 潮流小物 | 实物 | ¥59 |
| 8 | 潮玩盲盒 | 盲盒 | 实物 | ¥69 |
| 9 | 文具盲盒 | 盲盒 | 实物 | ¥39 |
| 10 | 小蛋糕兑换券 | 甜点鲜花 | 电子券 | ¥128 |
| 11 | 向日葵花束 | 甜点鲜花 | 实物 | ¥99 |

### 礼物 `Gift`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | u64 | 自增 |
| `code` | String | `LY-XXXX` |
| `dir` | u8 | 0 送出 Sent / 1 收到 Received |
| `item` | u16 | 目录下标 |
| `price` | i64 | 下单时价格（分），之后目录改价不影响 |
| `peer` | String | 送出：收礼人备注（可空）；收到：送礼人称呼（别名用 `/` 分隔） |
| `unlock` | u8 | 0 猜我是谁 / 1 私密问答 / 2 专属暗号 / 3 直接领取 |
| `clue` | String | 猜我是谁：线索；私密问答：问题；专属暗号：提示 |
| `answer` | String | 私密问答 / 专属暗号的答案；猜我是谁：送出时存我的称呼，收到时留空（用 `peer`） |
| `contract` | String | 空 = 无契约 |
| `message` | String | 寄语 |
| `sent_on` | i64 | 天序号 |
| `attempts` | u8 | 已用机会 |
| `state` | u8 | 见下 |
| `identity_known` | bool | 揭晓时是否知道送礼人（身份保密后同意契约会变成 true） |
| `solved` | bool | 答对（或直接领取）拆开的，而不是机会用完；时间线据此分两种说法，之后不变 |
| `opened_on` / `revealed_on` / `settled_on` | i64 | 0 = 未发生 |
| `swap_item` | u16 | 换购后的目录下标（`u16::MAX` = 无） |
| `refund` | i64 | 折现 / 换购 / 退回时回到余额的金额（分） |
| `voucher` | String | 电子券券码（收下时生成） |
| `ship_name` / `ship_phone` / `ship_addr` | String | 实物收件信息 |
| `demo_tip` | String | 演示提示（只在演示数据里有） |

`GiftState`：0 待拆 Sealed · 1 解谜中 Opened · 2 已揭晓 Revealed · 3 已收下 Accepted ·
4 已换购 Exchanged · 5 已折现 CashedOut · 6 已过期 Expired · 7 已撤回 Withdrawn。

送出的礼物中，收礼人的行为由模拟器写回同一条记录（答对与否、收下 / 换购 / 折现）。

### 契约 `Pact`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | u64 | |
| `gift_id` | u64 | 来源礼物 |
| `text` | String | 契约内容 |
| `peer` | String | 对方 |
| `mine` | bool | true = 我答应的（我是收礼人）；false = 答应我的 |
| `made_on` / `due_on` | i64 | `due_on = made_on + 7` |
| `state` | u8 | 0 待兑现 / 1 已兑现 / 2 已免除 |
| `nudged_on` | i64 | 最近一次「提醒 TA」的日期 |

### 流水 `LedgerEntry`

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | u64 | |
| `day` | i64 | |
| `amount` | i64 | 对余额的影响（分，带符号） |
| `external` | i64 | 这笔里模拟支付的部分（分，≥0，不影响余额） |
| `title` | String | 「送出 · 三顿半精品咖啡礼盒」 |
| `note` | String | 「手续费 ¥9」 |

**余额 = Σ amount**，不单独存，避免不一致。

### 熟人 `ContactLocal`

`id`、`label`（只有一个称呼）；统计「送过 / 收到」由礼物按 `peer` 名字匹配算出。
另存一份**通讯录** `directory: Vec<String>`（导入候选 / 猜人候选的名字），
可从 `$MAKEPAD_HOME/liyu/contacts.vcf` 导入 vCard。

### 设置 `Settings`

`nickname`（默认「阿岚」，猜我是谁时对方要猜的名字）、`notify_gift`、`notify_pact`、
`onboarded`、`theme`（`Option<String>`，存 `ThemeMode::id`；`None` = 没选过，按夜色）、
`ship_name` / `ship_phone` / `ship_addr`（上次填的收件信息，下次收实物时预填，只在本机）。

### 落盘结构 `PersistedState`

`version`、`contacts`、`directory`、`gifts`、`pacts`、`ledger`、`settings`、`next_id`，
全部是 `Option`：缺字段按默认值补，旧文件也能读。

## 持久化边界

| 存 | 不存 |
| --- | --- |
| 礼物、契约、流水、熟人、通讯录、设置、自增 id | 目录（代码常量）、当前页面 / 送礼草稿、拆礼阶段、toast、通知已弹标记 |

写盘时机：每次状态变更后立即写（数据量小）；「我 · 导出数据」把同一份写到
`liyu/export-<日期>.json`；读失败时用演示数据并 toast 一次「数据读不出来，已用演示数据」。

## 演示数据（`LiyuState::demo(today)`）

- 设置：称呼「阿岚」；
- 熟人：林舟、陈晓、许宁；通讯录（导入候选 / 猜人候选）：周子墨、林小满、黄一诺、吴凯文；
- 流水：新人礼金 +¥20（today − 10）；
- 收到的礼物：
  1. 林舟 / 舟舟 → 三顿半精品咖啡礼盒 ¥109，猜我是谁，线索「上周一起淋雨的那个人」，
     契约「下周找时间回请我喝一杯咖啡」，寄语「降温了，喝点热的」，today − 1，待拆；
  2. 陈晓 → 电影双人套票 ¥98，私密问答「我们第一次一起看的电影叫什么？」答案「星际穿越」，
     契约「周末陪我看一场电影」，today − 2，待拆；
  3. 许宁 → 潮玩盲盒 ¥69，专属暗号，提示「我们宿舍的口头禅」，答案「月亮不睡我不睡」，
     演示提示「暗号是：月亮不睡我不睡」，today − 5，待拆（2 天后过期，触发通知）；
  4. 陈晓 → 喜茶多肉葡萄兑换券 ¥29，直接领取，契约「收下要发一条朋友圈晒一晒」，
     today − 6，已收下（券码已发）；
- 送出的礼物：
  1. → 林舟：星巴克中杯拿铁电子券 ¥35，猜我是谁，today − 3，已揭晓（身份已知）；
  2. → 许宁：香薰蜡烛 ¥88，私密问答，契约「下次见面先给我一个拥抱」，today − 8，已收下，
     生成契约（答应我的，today − 6 起，due today + 1）；
  对应流水：送出（余额抵 ¥20 + 模拟支付）两条；
- 契约：送出 2 生成的一条「答应我的」，收到 4 生成的一条「我答应的」
  「收下要发一条朋友圈晒一晒」对陈晓，today − 6 起，due today + 1（触发契约通知）。

## 演示模拟器

「送出详情 · 模拟 TA 的下一步」推进一步，结果只由礼物 id 决定（`seed = id * 2654435761`），
便于测试复现。三个判断各取乘积高处的一段（低位只是 id 的余数）：
`g = (seed >> 16) % 4`、`d = (seed >> 20) % 3`、`r = (seed >> 24) % 2`。

| 当前 | 下一步 |
| --- | --- |
| 待拆 | 解谜中（`attempts = 1`，猜错一次） |
| 解谜中 | g ≠ 0：答对，已揭晓（身份已知）；否则机会用完，已揭晓（身份保密） |
| 已揭晓 | d：0 → 已收下（有契约则生成「答应我的」契约）；1 → 已换购（换成目录下一件）；2 → 已折现 |
| 终态 | 不动 |

对方折现后，若 r == 0，额外生成一份**回礼**（收到的礼物，来自同一个人，
目录里不超过刚折现金额的最贵一件，玩法「直接领取」，寄语「收到你的礼物啦，礼尚往来」）——
演示「反击礼物」的传播闭环。
